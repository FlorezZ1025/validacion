sonar.projectKey=FlorezZ1025_validacion
sonar.organization=florezz1025

# This is the name and version displayed in the SonarCloud UI.
#sonar.projectName=validacion
#sonar.projectVersion=1.0

sonar.projectKey=juanpabl0svn_social-media-app
sonar.organization=juanpabl0svn

# This is the name and version displayed in the SonarCloud UI.
sonar.projectName=social-media-app
sonar.projectVersion=1.0

# Exclude specific files and directories from analysis and coverage
sonar.exclusions=**/dist/**, **/coverage/**, **/node_modules/**, **/*.spec.ts, **/skeletons/**, **/*.module.ts, **/*.config.*.ts,**/main.ts, **/prisma/**, **/_firebase/**, **/app.router.ts, **/constants.ts, **/svg/**, **/test/**, **/*.json, **/*.dto.ts, **/*.entity.ts, **/*.interface.ts, **/*.guard.ts, **/*.pipe.ts, **/*.html, **/*.css, **/main.*.ts, **/*.config.*.js, **/*.config.ts, **/*.config.js, **/server.ts, **/*.routes.ts, **/models/**, **/app.component.ts, **/Dockerfile, **/cypress/**



# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
#sonar.sources=.

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8