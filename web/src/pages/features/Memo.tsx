/* eslint-disable prettier/prettier */
export async function ToFile(){
    const fakeApiResponse = await Promise.resolve("Archived successfully");
    return fakeApiResponse;
}

export async function DeleteMemo(){
    const fakeApiResponse = await Promise.resolve("Deleted Succesfully");
    return fakeApiResponse;
}

export async function CopyLink(){
    const fakeApiResponse = await Promise.resolve("Enlace copiado correctamente");
    return fakeApiResponse;
}